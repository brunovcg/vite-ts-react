import { LoadingSpinner } from "./LoadingSpinner.component";
import type { ComponentDoc } from "@/types/component-doc.types";

export const loadingSpinnerDoc: ComponentDoc = {
  id: "loading-spinner",
  name: "LoadingSpinner",
  description: "An animated loading spinner.",
  component: LoadingSpinner,
  args: {},
  argTypes: {},
};
