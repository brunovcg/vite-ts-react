import { locales } from "@/locales";

export const headerLocale = locales.create({ ptBR: { logout: "Logout", preferences: "Preferências" }, enUS: { logout: "Logout", preferences: "Preferences" } });
